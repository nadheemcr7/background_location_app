const locationConfig = {
    // You can change these later
    minTimeMs: 5 * 60 * 1000,  // 5 minutes
    minDistanceM: 10,          // 10 meters
    notifTitle: 'Tracking location',
    notifText: 'Background location is active',
  };
  
  export default locationConfig;

  